import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: '/TO-DO-s-App-using-ReactJS/', // Adjust this to match your repository name
});
